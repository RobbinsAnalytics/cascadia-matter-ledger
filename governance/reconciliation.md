# Live increment vs frozen baseline

*Generated by `src/reconcile_live_edge.py`. Regenerate it; never
hand-edit it. Machine-readable form: `governance/health.json`.*

The slice, in both vocabularies: **N.D. Cal.**, contract nature of suit
**190**, filed on or after **2024-01-01**. The live source calls that court
`cand`; the frozen source calls it circuit 9 district 71 and has never
heard of the string `cand`. Mapping between them is a conformance step,
not a lookup, and it is stated here rather than buried in a join.

| | Count |
|---|---:|
| Frozen baseline — matters in slice | **954** |
| Frozen baseline — of those, still open | 384 |
| Live edge — dockets on roster | **332** |
| Live edge — dockets fully ingested | 120 |
| Live edge — dockets partially ingested | 0 |
| Live edge — docket entries derived | 4,611 |

**Variance, roster against frozen baseline: -622.**

## It does not balance, and it is not supposed to

A reconciliation that always balances is not being computed. These are
the reasons this one does not, in order of size:

**1 · The roster is complete, and the gap is not a backlog.**
`roster_complete` is `True`. The slice has been enumerated down to a
derived id floor -- see `live-edge-design.md` W-05 -- so the 332 dockets
on the roster are every docket in this slice the source will return. The
variance below is therefore structural rather than a queue that drains,
and it moves only as new cases are filed.

**2 · The two sources count different things.** The frozen IDB is a
case-level administrative record of every civil case reported to the
Administrative Office. RECAP holds what somebody purchased or
contributed from PACER. Neither is a subset of the other.

**3 · The freeze lags.** The IDB is refreshed quarterly and this
snapshot was taken 2026-08-26; a case filed last week is in RECAP and
not in the freeze. The live edge is, by construction, ahead.

**4 · Nature of suit is coded differently.** The frozen source stores a
three-digit code. The live source stores free text that happens to
begin with it. `istartswith` bridges them and would not survive a court
that formats the field differently.

## The derived events

| Event type | Entries |
|---|---:|
| `UNCLASSIFIED` | 3,481 |
| `ORDER` | 295 |
| `NOTICE` | 201 |
| `MOTION` | 190 |
| `RESPONSE` | 103 |
| `COMPLAINT` | 88 |
| `SUMMONS` | 88 |
| `STIPULATION` | 83 |
| `TRANSCRIPT` | 33 |
| `DECLARATION` | 27 |
| `ANSWER` | 20 |
| `JUDGMENT` | 2 |

**`UNCLASSIFIED` is 75.5% and that is a health metric, not a bug.**
Most of it is entries with no description text at all — RECAP holds
the docket line without the document. They are retained and counted
rather than dropped, because an entry with no text is still evidence
that something happened, and discarding it would silently shrink
every denominator. See `docket-event-derivation.md` D-04 and D-05.

## Last run

| | |
|---|---|
| Started | 2026-08-31T14:10:28+00:00 |
| Status | **ok** |
| Assertions | 24, 0 failed |

A run that stops early because the rate limit bound it is recorded as
stopped, not as failed, and it says which window bound it. A run that
fails an assertion is recorded as failed and publishes which one. The
distinction is deliberate: a pipeline that reports designed
backpressure as failure trains its reader to ignore red.
